/**
 * Creates a set from an array
 * @param { Object[] } array - the array to create set from
 * @returns { Object () } - a set with unique values from array
 */
export default function setFromArray(array) {
  return new Set(array);
}
